# Port for the Spring Boot app
server.port=8080

# Vertex AI Endpoint Configuration
vertex.api.project-id=${VERTEX_AI_PROJECT_ID}
vertex.api.endpoint-id=${VERTEX_AI_ENDPOINT_ID}
vertex.api.location=us-central1

# Optional: Access token (if not using gcloud or VERTEX_AI_ACCESS_TOKEN env var)
# vertex.api.access-token=${VERTEX_AI_ACCESS_TOKEN}

