# Port for the Spring Boot app
server.port=8080

# Vertex AI Endpoint Configuration
vertex.api.project-id=559219752221
vertex.api.location=us-central1

# Model Endpoint IDs
vertex.api.endpoint-id.himu-1=2649496467986710528
vertex.api.endpoint-id.himu-2=6252376169883107328

# vertex.api.access-token=

# Optional: Access token (if not using gcloud or GOOGLE_ACCESS_TOKEN env var)
# The service will automatically use gcloud auth print-access-token if available
# Or set GOOGLE_ACCESS_TOKEN environment variable
# vertex.api.access-token=${GOOGLE_ACCESS_TOKEN}

